name: Przygotowanie 4J.2

on:
  push:
    branches: [work/4j-2-publikacja]

permissions:
  contents: write

jobs:
  przygotowanie:
    runs-on: ubuntu-latest
    steps:
      - name: Pobierz repozytorium
        uses: actions/checkout@v4
        with:
          ref: work/4j-2-publikacja

      - name: Wprowadź 4J.2
        shell: bash
        run: |
          set -euo pipefail
          python .github/4j2_patch.py

      - name: Uruchom pełną regresję
        shell: bash
        run: |
          set -euo pipefail
          for plik in testy/*.test.js; do
            echo "=== $plik ==="
            node "$plik"
          done

      - name: Zapisz gotowy pakiet 4J.2
        shell: bash
        run: |
          set -euo pipefail
          rm -f .github/4j2_patch.py .github/workflows/4j2_prepare.yml
          git config user.name "PawelP"
          git config user.email "53014565+aprofirm@users.noreply.github.com"
          git add -A
          git commit -m "4J.2: publikacja i weryfikacja Pages"
          git push origin HEAD:work/4j-2-publikacja
